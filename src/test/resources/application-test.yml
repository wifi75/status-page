spring:
  datasource:
    # un database nuovo per ogni contesto di test
    url: jdbc:sqlite:target/test-${random.uuid}.db

status:
  # i test non devono fare controlli di rete
  scheduler-enabled: false
